name: MissionOps CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run safety and workflow tests
        run: python -m unittest discover -s tests -v

      - name: Run synthetic golden case
        run: python engine/missionops.py > /tmp/missionops-run.json
